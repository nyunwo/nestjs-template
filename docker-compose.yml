version: "3.6"
services:
  nest-tpl:
    build: .
    container_name: nest-tpl
    environment:
      NODE_ENV: "development"
    deploy:
      #resources:
      #  limits:
      #    memory: 1000M
      #restart_policy:
      #  condition: on-failure
    ports:
      - "3002:3002"
    networks:
      - nest-tpl
    #network_mode: host
    volumes:
      - ./:/app

networks:
   nest-tpl:
#volumes:
#  data-nest:
