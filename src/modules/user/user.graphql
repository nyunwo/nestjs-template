type Query {
    getUser(id: Int): User
}

type Mutation {
    addUser(userData: UserInput): User
}

type User {
  id: Int!
  password: String
  salt: String
  username: String!
}

input UserInput{
  username: String!
  password: String
}